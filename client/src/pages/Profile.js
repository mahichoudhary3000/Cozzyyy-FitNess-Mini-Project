import React, { useState } from "react";
import { useAuth } from "../context/AuthContext";
import { api } from "../services/api";

export default function Profile(){const {user,setUser}=useAuth();const [form,setForm]=useState({name:user.name,age:user.age,gender:user.gender,height:user.height,weight:user.weight});const [msg,setMsg]=useState("");const submit=async e=>{e.preventDefault();try{const r=await api.updateUser(user._id,{...form,age:Number(form.age),height:Number(form.height),weight:Number(form.weight)});setUser(r.user);setMsg("Profile updated successfully.");}catch(e){setMsg(e.message)}};return <section className="section narrow"><p className="eyebrow">PROFILE</p><h1>Your profile</h1><form className="panel" onSubmit={submit}><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input value={user.email} disabled/></label><div className="form-grid"><label>Age<input value={form.age} onChange={e=>setForm({...form,age:e.target.value})}/></label><label>Gender<input value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}/></label><label>Height<input value={form.height} onChange={e=>setForm({...form,height:e.target.value})}/></label><label>Weight<input value={form.weight} onChange={e=>setForm({...form,weight:e.target.value})}/></label></div>{msg&&<div className="success">{msg}</div>}<button className="btn primary">Update Profile</button></form></section>}
